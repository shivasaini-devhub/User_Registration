pipeline {
    agent any

    environment {
        AWS_REGION = 'eu-west-1'
        ECR_REPOSITORY = '804837308178.dkr.ecr.eu-west-1.amazonaws.com/user-backend'
        IMAGE_TAG = "${BUILD_NUMBER}"
    }

    stages {

        stage('Checkout') {
            steps {
                git(
                    branch: 'main',
                    credentialsId: 'github-user-registration',
                    url: 'https://github.com/shivasaini-devhub/User_Registration.git'
                )
            }
        }

        stage('Install Dependencies') {
            steps {
                dir('backend') {
                    sh 'npm ci'
                }
            }
        }

        stage('Run Tests') {
            steps {
                dir('backend') {
                    sh 'npm test'
                }
            }
        }

        stage('Build Application') {
            steps {
                dir('backend') {
                    sh 'npm run build'
                }
            }
        }

        stage('Docker Build') {
            steps {
                sh '''
                    docker build \
                        -t ${ECR_REPOSITORY}:${IMAGE_TAG} \
                        ./backend
                '''
            }
        }

        stage('AWS ECR Login') {
            steps {
                withAWS(
                    credentials: 'aws-ecr',
                    region: "${AWS_REGION}"
                ) {
                    sh '''
                        aws sts get-caller-identity

                        aws ecr get-login-password \
                            --region ${AWS_REGION} | \
                        docker login \
                            --username AWS \
                            --password-stdin \
                            804837308178.dkr.ecr.eu-west-1.amazonaws.com
                    '''
                }
            }
        }

        stage('Push Image to ECR') {
            steps {
                sh '''
                    docker push ${ECR_REPOSITORY}:${IMAGE_TAG}
                '''
            }
        }
    }

    post {
        success {
            echo "Backend image pushed successfully: ${ECR_REPOSITORY}:${IMAGE_TAG}"
        }

        failure {
            echo 'Babkend pipeline failed.'
        }

        always {
            cleanWs()
        }
    }
}
